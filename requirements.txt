# LeftSize Runner Dependencies
# All dependencies pinned with exact versions for security

# Cloud Custodian for Azure and AWS resource scanning
c7n==0.9.40.0
c7n-azure==0.7.40.0

# Azure SDK for authentication and resource management
azure-identity==1.19.0
azure-mgmt-core==1.4.0

# HTTP requests for posting findings to backend
requests==2.32.3

# JSON schema validation for configuration
jsonschema==4.23.0

# YAML configuration parsing - must match Cloud Custodian requirement
pyyaml==6.0.1

# CLI argument parsing
click==8.1.7

# Structured logging
structlog==24.4.0

# Note: For production, generate hashes with:
# pip-compile --generate-hashes requirements.in
# For now using exact version pins
